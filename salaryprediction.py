# -*- coding: utf-8 -*-
"""salaryprediction.ipynb

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/1vKghu-Rv_WBOQhO9WX4RhwFN4CJqHssp
"""

import csv

with open('hiring.csv', 'w', newline='') as file:
    writer = csv.writer(file)
    field = ["experience", "test_score", "interview_score", "salary"]

    writer.writerow(field)
    writer.writerow(["nan",8,9,50000])
    writer.writerow(["nan",8,6,45000])
    writer.writerow(["five",6,7,60000])
    writer.writerow(["two",10,10,65000])
    writer.writerow(["seven",9,6,70000])
    writer.writerow(["three",7,10,62000])
    writer.writerow(["ten",'nan',7,72000])
    writer.writerow(["eleven",7,8,80000])

import pandas as pd

pd.read_csv('hiring.csv')

pd.to_save_csv('hiring.csv')

# Importing the libraries
import numpy as np
import matplotlib.pyplot as plt
import pandas as pd
import pickle

dataset = pd.read_csv('hiring.csv')

dataset['experience'].fillna(0, inplace=True)
dataset['experience']

dataset['test_score'].fillna(dataset['test_score'].mean(), inplace=True)
dataset['test_score']

X = dataset.iloc[:, :3]
X

#Converting words to integer values
def convert_to_int(word):
    word_dict = {'one':1, 'two':2, 'three':3, 'four':4, 'five':5, 'six':6, 'seven':7, 'eight':8,
                'nine':9, 'ten':10, 'eleven':11, 'twelve':12, 'zero':0, 0: 0}
    return word_dict[word]

X['experience'] = X['experience'].apply(lambda x : convert_to_int(x))

y = dataset.iloc[:, -1]

#Splitting Training and Test Set
#Since we have a very small dataset, we will train our model with all availabe data.

from sklearn.linear_model import LinearRegression
regressor = LinearRegression()

#Fitting model with trainig data
regressor.fit(X.values, y)

# Saving model to disk
pickle.dump(regressor, open('model.pkl','wb'))

# Loading model to compare the results
model = pickle.load(open('model.pkl','rb'))
print(model.predict([[2, 9, 6]]))

